#!/bin/bash

echo "########### LOCAL REPO HOOK ###############"

mkdir -p /opt/repo/pool
cd /opt/repo/pool

# download grub packages + deps without installing
apt-get download \
    grub-efi \
    grub-legacy \
    shim-signed

ls -lh .
exit 1
